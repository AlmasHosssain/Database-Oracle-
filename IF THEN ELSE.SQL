SET SERVEROUTPUT ON;
DECLARE 
  V_NUMBER NUMBER := &ENTER_THE_NUMBER;
BEGIN
  IF MOD(V_NUMBER,2) = 0 THEN
  DBMS_OUTPUT.PUT_LINE(V_NUMBER||' IS EVEN');
  ELSE
  DBMS_OUTPUT.PUT_LINE(V_NUMBER||' IS ODD');
  END IF;
  DBMS_OUTPUT.PUT_LINE('IF THEN ELSE COMPLETE SUCCESSFULLY');
END;