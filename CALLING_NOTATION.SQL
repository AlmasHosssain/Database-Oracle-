SET SERVEROUTPUT ON;
CREATE TABLE TFR(
  SALARY NUMBER,
  NAM VARCHAR2(30)
);
INSERT INTO TFR(SALARY,NAM) VALUES(100000,'ALMAS');
INSERT INTO TFR(SALARY,NAM) VALUES(200000,'HOSSAIN');
INSERT INTO TFR(SALARY,NAM) VALUES(500000,'ANATR');

CREATE OR REPLACE PROCEDURE TST (V_NAME VARCHAR2,V_PAY NUMBER) IS
BEGIN
  UPDATE TFR SET SALARY = salary*V_PAY WHERE NAM=V_NAME;
  DBMS_OUTPUT.PUT_LINE('BOSS WORK IS DONE');
END;
/
EXECUTE TST('ALMAS',2.67);

CREATE OR REPLACE FUNCTION ADD_NUM(NUM1 NUMBER,NUM2 NUMBER DEFAULT 1,
NUM3 NUMBER)
RETURN NUMBER IS
V_RESULT NUMBER;
BEGIN
  DBMS_OUTPUT.PUT_LINE('NUM1->'||NUM1);
  DBMS_OUTPUT.PUT_LINE('NUM2->'||NUM2);
  DBMS_OUTPUT.PUT_LINE('NUM3->'||NUM3);
  
  V_RESULT:= NUM1+NUM2+NUM3;
  RETURN V_RESULT;
END;
/

BEGIN 
  DBMS_OUTPUT.PUT_LINE(ADD_NUM(NUM3=>7,NUM1=>3));
END;
/














