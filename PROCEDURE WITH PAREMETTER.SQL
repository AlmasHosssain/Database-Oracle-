
DESC EDT;
CREATE OR REPLACE PROCEDURE SEC_PROC(V_ID NUMBER,RASE NUMBER)
IS
V_SALARY EDT.SALARY%TYPE;
BEGIN
  UPDATE EDT SET SALARY=SALARY * RASE WHERE
  CUS_ID = V_ID;
  SELECT SALARY INTO V_SALARY FROM DUAL; 
  --DBMS_OUTPUT.PUT_LINE(SALARY);
END;
