SET SERVEROUTPUT ON;
DECLARE
  V_VAR VARCHAR2(400);
BEGIN
  V_VAR := 'CREATE TABLE KJY(
          F_NAME VARCHAR2(30),
          L_NAME VARCHAR2(30),
          SALARY NUMBER
          )';
  EXECUTE IMMEDIATE V_VAR;
END;
/
DESC KJY;
/
DECLARE
 V_VAR VARCHAR2(300);
BEGIN
  V_VAR:=' CREATE TABLE HG('||
    'F_NAME VARCHAR2(30),'||
    'ID_X NUMBER,'||
    'L_ANAME VARCHAR2(30),'||
    'CONSTRAINT KOP PRIMARY KEY (ID_X)'||
  ')';
 EXECUTE IMMEDIATE V_VAR;
END;
/
DESC HG;
/
DECLARE
 V_VAR VARCHAR2(200);
  V_VAR1 VARCHAR2(200);
BEGIN
  V_VAR:= 'ALTER TABLE HG ADD V_CITY VARCHAR2(30)';
  V_VAR1:= 'DROP TABLE HG';
  EXECUTE IMMEDIATE V_VAR1;
  EXECUTE IMMEDIATE V_VAR;
END;
/
DESC HG;














